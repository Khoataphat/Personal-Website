// 1. Khởi tạo Swup cho chuyển trang mượt mà
document.addEventListener('DOMContentLoaded', function() {

    const swup = new Swup();

    // 2. Khởi tạo Typed.js

    var typed = new Typed('.auto-type', {
        strings: ['Dang Khoa'],
        typeSpeed: 50,
        backSpeed: 50,
        loop: true
    });
});